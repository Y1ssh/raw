## What & why

## Base vs pack check
- [ ] This change is COMMON to every agent (belongs in the base). If not, it belongs in a pack repo.

## Checklist
- [ ] Tests added/updated
- [ ] `make lint` passes (ruff + mypy)
- [ ] No secrets committed
- [ ] Docs updated if behavior changed
