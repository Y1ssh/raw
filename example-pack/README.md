# example-pack (smoke test)

The smallest possible pack — one dummy tool. Its only job is to prove the base's pack loader
discovers + registers a pack. Real packs (Grounded etc.) live in their OWN repos.

Swap `RAW_PACK_PATH=./example-pack` to something else -> a different agent.
