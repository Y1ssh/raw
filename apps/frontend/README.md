# Raw frontend (the cockpit shell)

The generic, UNSKINNED agent shell — chat + canvas + streaming + autonomy slider + threads.
Packs supply the skin (theme tokens + domain components). Built in Sprint 3.

Planned stack: Next.js 15 + React 19 + TypeScript + assistant-ui + CopilotKit + shadcn/ui + Tailwind v4.
